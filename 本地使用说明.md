# 本地使用说明

本文面向希望在本机运行 wechat-content-pipeline 的用户。所有命令都应在仓库根目录执行。

## 1. 初始化环境

    python -m venv .venv
    .\.venv\Scripts\Activate.ps1
    python -m pip install -r requirements.txt
    Copy-Item .env.example .env

编辑 .env：

- 选题与写作：CONTENT_API_KEY，可与 APIMART_API_KEY 共用；
- 生图：APIMART_API_KEY；
- 公众号草稿：WECHAT_ACCESS_TOKEN，或 WECHAT_APP_ID + WECHAT_APP_SECRET。

.env 只保留在本机，不要提交到 Git，也不要粘贴进日志或项目文档。

## 2. 安装 Skill

    .\install-local.ps1

安装“公众号白底极简风格公众号文章”：

    .\install-local.ps1 -SkillName wechat-white-minimal-article

更新已安装的白底极简 Skill：

    .\install-local.ps1 -SkillName wechat-white-minimal-article -Force

也可以不安装，直接设置对应脚本目录：

    $scripts = ".\skill\wechat-content-pipeline\scripts"
    $scripts = ".\skill\wechat-white-minimal-article\scripts"

## 3. 准备一个系列目录

    $source = "D:\资料\guide.pdf"
    $series = ".\output\guide-series"

支持 PDF、Markdown 和纯文本。来源资料始终按只读输入处理。

## 4. 规划选题

    python "$scripts\pipeline.py" topics --source $source --series-dir $series --count 10 --env-file ".\.env"

审核：

- topic_plan.json；
- 选题矩阵.md；
- 每篇是否只有一个核心承诺；
- 来源引用是否有效；
- 选题是否重复；
- 内容是否足以拆成 1 张封面和 5 张内容图。

使用白底极简 Skill 时，`title` 与 `compact_title` 都必须保存完整标题 `每天介绍一个 AI 产品：{产品名}`，最长 32 字，不得改成“AI产品：{产品名}”。

## 5. 写正文和六图指令

    python "$scripts\pipeline.py" content --source $source --series-dir $series --env-file ".\.env"

每篇生成：

- 来源卡.md；
- 公众号终稿_图片标注版.md；
- 贴图指令.md。

脚本会跳过完整目录。只在明确需要重做时加入 --force。

## 6. 生成图片

先解析任务，不产生付费请求：

    python "$scripts\pipeline.py" images --series-dir $series --env-file ".\.env"

确认任务数等于“文章数 × 6”后执行：

    python "$scripts\pipeline.py" images --series-dir $series --env-file ".\.env" --execute-images

生图状态写入 image_generation_manifest.json。中断后直接重跑，不要删除 manifest，也不要默认加入 --force。

## 7. 保存到公众号草稿箱

先验证清单，不写公众号：

    python "$scripts\pipeline.py" drafts --series-dir $series --env-file ".\.env"

确认目标公众号和 IP 白名单后保存草稿：

    python "$scripts\pipeline.py" drafts --series-dir $series --env-file ".\.env" --save-drafts

这里只创建 article_type 为 newspic 的草稿，不正式发表、不群发。

## 8. 最终验收

    python "$scripts\pipeline.py" audit --series-dir $series

通过标准：

- 选题数、文章目录数和草稿数一致；
- 每篇三个 Markdown 文件齐全；
- 每篇正好六张贴图；
- 草稿均为 saved；
- 草稿标识全部唯一；
- failed=0、needs_update=0、published=0。

只检查本地产物时：

    python "$scripts\audit_pipeline.py" --series-dir $series --allow-local-only

## 9. 恢复与排错

### 微信返回 40164

把当前出口 IP 加入公众号后台 IP 白名单，然后重跑草稿阶段。不要把具体 IP 写入仓库。

### 标题显示字面量 Unicode

保留已创建草稿的 MediaID。脚本会用显式 UTF-8 JSON 原位更新，避免创建重复草稿。

### 后台标题被错误缩写

以每篇 `贴图指令.md` 的“公众号标题”为准，更新标题映射和草稿清单；保留原 `draft_media_id`，把结果状态设为 `needs_update` 后重跑草稿阶段。脚本通过 `draft/update` 原位修正并用 `draft/get` 回查，不新建替代草稿。

### 生图中断

保留 image_generation_manifest.json 并重跑 images 阶段。已完成图片会被跳过。

### 草稿回查失败

保留 公众号贴图草稿结果.json。如果已经拿到草稿标识，重跑只应更新同一草稿。

### 需要重做单篇

优先只移除该篇的失败产物或状态，避免对整个系列使用 --force。

## 10. 推荐的审核节奏

以 10 篇为一批：

1. 先审核选题矩阵；
2. 再审核正文与六图指令；
3. 先试生成一篇六图，确认视觉风格；
4. 批量生图；
5. 先保存一篇公众号草稿，确认贴图顺序和标题；
6. 批量保存草稿；
7. 运行审计。

这能把内容判断、付费操作和账号写入拆成三个清晰检查点。
