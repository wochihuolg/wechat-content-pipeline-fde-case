# 本地使用说明

## 1. 安装

```powershell
cd "F:\CODEX\阅读代码仓库\wechat-content-pipeline-fde-case"
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
Copy-Item .env.example .env
```

编辑 `.env`：

- 选题与写作：`CONTENT_API_KEY`，可与 `APIMART_API_KEY` 共用。
- 生图：`APIMART_API_KEY`。
- 公众号草稿：`WECHAT_ACCESS_TOKEN`，或 `WECHAT_APP_ID` +
  `WECHAT_APP_SECRET`。

不要把 `.env` 提交到 Git。

## 2. 安装 Skill

```powershell
.\install-local.ps1
```

也可以不安装，直接调用：

```powershell
$scripts = ".\skill\wechat-content-pipeline\scripts"
```

## 3. 选题

```powershell
python "$scripts\pipeline.py" topics `
  --source "D:\资料\guide.pdf" `
  --series-dir ".\output\2026-07-29\guide-series" `
  --count 10 `
  --env-file ".\.env"
```

审核：

- `topic_plan.json`
- `选题矩阵.md`
- 每篇是否只有一个承诺；
- 来源引用是否有效；
- 选题是否重复；
- 是否足以拆成 1 张封面 + 5 张内容图。

## 4. 写正文和六图指令

```powershell
python "$scripts\pipeline.py" content `
  --source "D:\资料\guide.pdf" `
  --series-dir ".\output\2026-07-29\guide-series" `
  --env-file ".\.env"
```

每篇生成：

- `来源卡.md`
- `公众号终稿_图片标注版.md`
- `贴图指令.md`

脚本会跳过完整目录。只有明确需要重做时才加 `--force`。

## 5. 生图

先只解析任务：

```powershell
python "$scripts\pipeline.py" images `
  --series-dir ".\output\2026-07-29\guide-series" `
  --env-file ".\.env"
```

确认任务数为“文章数 x 6”后执行：

```powershell
python "$scripts\pipeline.py" images `
  --series-dir ".\output\2026-07-29\guide-series" `
  --env-file ".\.env" `
  --execute-images
```

生图状态写入 `image_generation_manifest.json`，中断后直接重跑。

## 6. 保存到公众号草稿箱

先验证清单，不写公众号：

```powershell
python "$scripts\pipeline.py" drafts `
  --series-dir ".\output\2026-07-29\guide-series" `
  --env-file ".\.env"
```

确认公众号目标账号和 IP 白名单后保存草稿：

```powershell
python "$scripts\pipeline.py" drafts `
  --series-dir ".\output\2026-07-29\guide-series" `
  --env-file ".\.env" `
  --save-drafts
```

这一步只创建 `article_type: "newspic"` 草稿，不正式发表、不群发。

## 7. 最终验收

```powershell
python "$scripts\pipeline.py" audit `
  --series-dir ".\output\2026-07-29\guide-series"
```

通过标准：

- 选题数、文章目录数、草稿数一致；
- 每篇 3 个 Markdown 文件齐全；
- 每篇正好 6 张贴图；
- 草稿均为 `saved`；
- 草稿标识全部唯一；
- `failed=0`、`needs_update=0`、`published=0`。

## 8. 常见恢复

- `40164`：把当前出口 IP 加入公众号后台 IP 白名单。
- 标题出现 `\uXXXX`：保留草稿 MediaID，重跑会用 UTF-8 原位更新。
- 生图中断：保留 manifest，直接重跑，不加 `--force`。
- 草稿回查失败：保留结果 JSON，重跑只更新同一草稿，不重复创建。
- 只想检查本地产物：运行 `audit_pipeline.py --allow-local-only`。
